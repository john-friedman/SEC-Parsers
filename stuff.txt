TODO:
start making tree, do basic logic. once this part is done, check how well it worked
if good publish

to fix: 
fix missing lhs of header - difficut
add ability for detect styole from element to look up parents, e.g. bold / strong

if no text below before next header asusme parent?
is sentence combined with italics etc
is first child?
# lookup what this is called
#tree  = etree.fromstring('<html> <body> <div>text1 <p> par1</p> text2 <p>par2</p> text3</div> </body> </html>', parser)

TODO:
use relative stuff after style detection

e.g. style detection gets you likely stuff
then we check nearest neighbor stuff
e.g. note+ unique style neighbor gets merged into desc